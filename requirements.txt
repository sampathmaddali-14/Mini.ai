# Mini.ai — Python requirements for policy code and helper scripts.
# The main services (OpenClaw, MemOS, OpenSpace) run as containers and
# bring their own dependencies. This file covers only the Python layer
# we maintain ourselves.

httpx>=0.27,<1.0          # MemOS HTTP client
pydantic>=2.5,<3.0        # data validation (if policies grow)
python-dotenv>=1.0        # .env loading for local dev
click>=8.1                # CLI ergonomics (future inspector UI)

# Embeddings / clustering for consolidation policy (Phase 2)
# sentence-transformers>=2.7
# hdbscan>=0.8
# numpy>=1.26

# Tests (dev)
pytest>=8.0
pytest-asyncio>=0.23
